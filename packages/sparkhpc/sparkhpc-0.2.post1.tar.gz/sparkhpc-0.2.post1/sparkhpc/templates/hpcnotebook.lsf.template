#!/bin/sh
#BSUB -J {jobname}
#BSUB -W {walltime} # request {walltime} HH:MM 
#BSUB -n {ncores} # requesting {ncores} cores for the driver
#BSUB -R light # run in the "light" queue
#BSUB -R rusage[mem={memory}] # memory request
#BSUB -o {jobname}-%J.log

hpcnotebook launch 
