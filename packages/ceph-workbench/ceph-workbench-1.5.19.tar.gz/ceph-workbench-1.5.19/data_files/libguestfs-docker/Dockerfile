# This Dockerfile is used to build an image containing basic stuff to be used as a Jenkins slave build node.
FROM fedora:21
MAINTAINER Loic Dachary <loic@dachary.org>

RUN yum install -y libguestfs-tools openssh wget
