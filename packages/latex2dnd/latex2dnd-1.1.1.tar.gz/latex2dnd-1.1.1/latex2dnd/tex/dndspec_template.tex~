% 
% DND tex file generated from <FILENAME>
%

\documentclass{article}
\usepackage{amsmath}
\input{latex2dnd}

\def\lbL{\lb\rule{0pt}{2.4ex}}
\def\lpL{\left(\rule{0pt}{2.4ex}}
\def\lb{\left[}         \def\rb{\right]}
\def\lp{\left(}         \def\rp{\right)}
\def\rar{\rightarrow}
\def\del{\nabla}
\def\w{\omegaup}
\def\ket#1{\left\vert #1 \right\rangle}
\def\bra#1{\left\langle #1 \right\vert}
\def\M#1#2{M_{#1}( #2) }
\def\R#1#2{R_{#1}( #2 ) }
\def\adag{a^\dagger}
\def\bdag{b^\dagger}
\def\Bdag{B^\dagger}

\def\bea{\begin{eqnarray}}
\def\eea{\end{eqnarray}}
\def\ben{\begin{eqnarray*}}
\def\een{\end{eqnarray*}}

\def\non{\nonumber}

\def\>{\rangle}
\def\<{\langle}
\def\l{\left}
\def\r{\right}

\newcommand{\ii}{{\rm i}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{document}

% \DDoptions{ALLOW_EMPTY}

\DDoptions{HIDE_FORMULA_INPUT}

% define drag-drop labels

\DDlabel[1]{one}{$1$}
\DDlabel[2]{two}{$2$}
\DDlabel[3]{three}{$3$}
\DDlabel{four}{$4$}
\DDlabel{five}{$5$}
\DDlabel{six}{$6$}

\DDlabel{hk}{$(\hbar k)$}
\DDlabel{hksq}{$(\hbar k)^2$}
\DDlabel{hkcu}{$(\hbar k)^3$}

% shorthand macro to make all boxes the same size (6 by 4)
\newcommand\DDB[2]{\DDbox{#1}{8ex}{4ex}{#2}}
\newcommand\DDBB[2]{\DDbox{#1}{22ex}{6ex}{#2}}
\newcommand\DDBBB[2]{\DDbox{#1}{4ex}{6ex}{#2}}
\newcommand\DDBBBB[2]{\DDbox{#1}{15ex}{6ex}{#2}}

\def\>{\rangle}

% the formula with boxes
\bea
 D^{em} = \frac{\DDB{1}{hksq}}{ \DDB{2}{five}}  \Gamma_s
\nonumber
\eea

% \DDformula{ ([1]) /( [2] * [3] * [4]) }{vp,f,amax@1,1,1:20,20,20\#20}{ vp^2 / (2*f*amax) }{}

% \DDtest{incorrect}{1,2,3,4,5,6}{vxcube,one,vfour,vxsq,two,vsq}
% \DDtest{correct}{1,2,3,4,5,6}{vxcube,vfour,two,vxsq,vsq,two}

% \DDformula{ ( ([1] * [2]) + ([3] * [4]) + ([5]*[6]) ) / ([7]) }{xaxb,yayb,zazb,q,R@1,1,1,1,1:20,20,20,20,20\#15}{ (xaxb + yayb - 2*zazb)/(R^3)  }{}

%$$\lambda = \frac{\DDB{1}{term1}\pm \sqrt{\DDB{2}{term2}\DDB{3}{fac}}}{\DDB{4}{ta}}$$

% output labels, with fixed box heights
\writeDDlabels[4.3ex]

\end{document}

